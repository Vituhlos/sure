---
en:
  activerecord:
    attributes:
      user:
        email: Email
        family: "%{moniker}"
        family_id: "%{moniker}"
        first_name: First Name
        last_name: Last Name
        password: Password
        password_confirmation: Password Confirmation
    errors:
      models:
        user:
          attributes:
            base:
              cannot_deactivate_admin_with_other_users: Admin cannot delete account
                while other users are present. Please delete all members first.
            profile_image:
              invalid_file_size: file size must be less than %{max_megabytes}MB
              invalid_format: must be a JPEG or PNG
            password:
              mixed_case_required: must include both uppercase and lowercase letters
              number_required: must include at least one number
              special_character_required: must include at least one special character
